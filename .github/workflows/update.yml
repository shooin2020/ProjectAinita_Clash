name: Build Clash Config

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'  # هر ۶ ساعت

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: pip install requests pyyaml

    - name: Run script
      run: python fetch_and_build_clash.py || (echo "💥 Script failed" && exit 1)

    - name: Commit and push changes
      env:
        PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
      run: |
        git config --global user.name "github-actions"
        git config --global user.email "github-actions@users.noreply.github.com"
        git add ProjectAinita_Clash.yaml || true
        git commit -m "Update Clash config" || echo "No changes to commit"
        git push https://x-access-token:${PAT_TOKEN}@github.com/shooin2020/ProjectAinita_Clash.git HEAD:main

